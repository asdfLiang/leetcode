package other.rotatearray.algorithm;

public class RotateSolution {
    /**
     * @param nums
     * @param k
     */
    public void rotate(int[] nums, int k) {
        int[] front = new int[k];

        for (int r = nums.length - 1; r >= (nums.length - k); r--) {
            // 记录最后一个数

            // 覆盖最后一个数

            // 覆盖前几位

        }

    }


}
